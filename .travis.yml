language: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install -r requirements/dev.txt
  - pip install pytest-cov
  - pip install python-coveralls
# command to run tests
script:
  py.test tests/ -v --cov coveralls --cov-report term-missing
after_success:
  coveralls